VITE_TAX_API_PROVIDER=taxjar
VITE_TAXJAR_API_KEY=

# Defaults to 'static' if not set. When set to 'taxjar' with a valid API key,
# the app will attempt to fetch state rates from the provider and fall back to static.

# Optional: used to pull live mortgage rates from FRED. When omitted we fall back to sample data.
VITE_FRED_API_KEY=
